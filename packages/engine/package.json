{
  "name": "@kauppalista/engine",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "bun test src/*.test.ts src/*.test.actual.ts",
    "test:actual:valio-kevyt-maito": "RUN_ACTUAL_ENGINE_TESTS=true KESKO_ACTUAL_TEST_TIMEOUT_MS=60000 S_GROUP_ACTUAL_TEST_TIMEOUT_MS=60000 bun test ./src/engine.test.actual.ts --test-name-pattern 'Valio kevyt maito'",
    "test:actual:cross-store-queries": "RUN_ACTUAL_ENGINE_TESTS=true KESKO_ACTUAL_TEST_TIMEOUT_MS=60000 S_GROUP_ACTUAL_TEST_TIMEOUT_MS=60000 bun test ./src/engine.test.actual.ts --test-name-pattern 'cross-store query smoke test'"
  },
  "dependencies": {
    "@kauppalista/db": "workspace:*",
    "@kauppalista/domain": "workspace:*",
    "@kauppalista/matcher": "workspace:*",
    "@kauppalista/searchers": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
