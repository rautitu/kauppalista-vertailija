{
  "name": "@kauppalista/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "db:migrate": "bun src/cli.ts migrate",
    "db:seed:test": "bun src/cli.ts seed",
    "test": "bun test src/db.integration.test.ts"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
